#!/usr/bin/env node\n# 📋 crypto-volatility - FEUILLE DE TRICHE\n\n## Installation rapide\n\n```bash\nnpm install crypto-volatility\n```\n\n## Imports\n\n```typescript\nimport { CoinGeckoProvider } from 'crypto-volatility';\nimport { VolatilityCalculator } from 'crypto-volatility';\nimport { DVOLCalculator } from 'crypto-volatility';\nimport type { CryptoSymbol, TimePeriod, DVOLMethod } from 'crypto-volatility';\n```\n\n## 1. Récupérer les données\n\n```typescript\nconst provider = new CoinGeckoProvider();\n\n// Récupérer les prix Bitcoin (30 jours)\nconst response = await provider.fetchPriceData('bitcoin', '30d');\n\nif (!response.success) {\n  console.error(response.error);\n  return;\n}\n\nconst priceData = response.data;\n```\n\n**Cryptos disponibles:** `'bitcoin'` | `'solana'`\n**Périodes:** `'1d'` | `'30d'` | `'365d'`\n\n## 2. Calculer la volatilité\n\n```typescript\n// Volatilité standard\nconst vol = VolatilityCalculator.calculateMetrics(priceData, '30d');\n\nconsole.log(vol.volatility);              // En %\nconsole.log(vol.annualizedVolatility);    // Annualisée en %\n```\n\n## 3. Calculer le DVOL\n\n### 3.1 Méthode EWMA (recommandée)\n\n```typescript\nconst dvol = DVOLCalculator.calculateDVOL(priceData, 'ewma');\n\nconsole.log(dvol.dvol);        // DVOL en %\nconsole.log(dvol.dvolIndex);   // Index 0-100\nconsole.log(dvol.confidence);  // Confiance 0-100%\n```\n\n### 3.2 Méthode Simple\n\n```typescript\nconst dvol = DVOLCalculator.calculateDVOL(priceData, 'simple', {\n  windowSize: 20\n});\n```\n\n### 3.3 Méthode GARCH\n\n```typescript\nconst dvol = DVOLCalculator.calculateDVOL(priceData, 'garch', {\n  garchParams: {\n    omega: 0.000001,\n    alpha: 0.1,\n    beta: 0.85\n  }\n});\n```\n\n## 4. Calculs individuels\n\n```typescript\nconst prices = priceData.map(d => d.price);\n\n// Rendements logarithmiques\nconst returns = VolatilityCalculator.calculateLogReturns(prices);\n\n// Moyenne\nconst mean = VolatilityCalculator.calculateMean(returns);\n\n// Variance\nconst variance = VolatilityCalculator.calculateVariance(returns);\n\n// Écart-type\nconst stdDev = VolatilityCalculator.calculateStandardDeviation(returns);\n\n// Volatilité annualisée\nconst annualized = VolatilityCalculator.annualizeVolatility(stdDev, 365);\n```\n\n## 5. Diagnostics\n\n```typescript\nconst diagnostics = DVOLCalculator.calculateDiagnostics(\n  priceData,\n  'ewma'\n);\n\nconsole.log(diagnostics.autocorrelation);    // Relation passée\nconsole.log(diagnostics.heteroskedasticity); // Volatilité changeante\nconsole.log(diagnostics.skewness);           // Asymétrie\nconsole.log(diagnostics.kurtosis);           // Queues grasses\n```\n\n## 6. Utiliser l'analyseur wrapper\n\n```typescript\nimport { VolatilityAnalyzer } from './examples/index.js';\n\nconst analyzer = new VolatilityAnalyzer();\n\n// Analyse simple\nconst result = await analyzer.analyze('bitcoin', '30d', 'ewma');\nconsole.log(result.dvol.dvol);\n\n// Comparer deux cryptos\nconst comp = await analyzer.compare('bitcoin', 'solana', '30d');\nconsole.log(`${comp.comparison.moreVolatile} est plus volatile`);\n\n// Analyser plusieurs cryptos\nconst multi = await analyzer.analyzeMultiple(\n  ['bitcoin', 'solana'],\n  '30d',\n  'ewma'\n);\nconsole.log(multi.ranking); // Classement par volatilité\n\n// Détecte anomalies\nconst anomalies = await analyzer.detectAnomalies('bitcoin', '30d', 2);\nconsole.log(`${anomalies.count} anomalies`);\n```\n\n## 7. Résultats typiques\n\n```typescript\n// Volatilité\n{\n  volatility: 2.14,              // %\n  variance: 45.78,\n  annualizedVolatility: 37.21    // %\n}\n\n// DVOL\n{\n  dvol: 2.15,                    // %\n  dvolIndex: 46.2,               // 0-100\n  confidence: 91.2,              // %\n  method: 'ewma',\n  dataPoints: 30,\n  calculatedAt: Date\n}\n```\n\n## 8. Interprétation DVOL Index\n\n| Range | Interprétation | Action |\n|-------|----------------|--------|\n| 0-25  | Très faible | Conditions stables |\n| 25-50 | Faible | Normal |\n| 50-75 | Modéré | Attention |\n| 75-100| Élevé | Alerte |\n\n## 9. Confiance\n\n| Range | Qualité | Recommandation |\n|-------|---------|----------------|\n| 90-100%| Excellent | Faire confiance |\n| 70-90% | Bon | Généralement fiable |\n| < 70%  | Faible | À valider |\n\n## 10. Gestion des erreurs\n\n```typescript\ntry {\n  const response = await provider.fetchPriceData('bitcoin', '30d');\n\n  if (!response.success) {\n    throw new Error(response.error);\n  }\n\n  const dvol = DVOLCalculator.calculateDVOL(response.data, 'ewma');\n} catch (error) {\n  console.error('Erreur:', error instanceof Error ? error.message : 'inconnue');\n}\n```\n\n## 11. Cas d'usage avancés\n\n### Alertes de volatilité\n\n```typescript\nconst analyzer = new VolatilityAnalyzer();\nconst result = await analyzer.analyze('bitcoin', '1d', 'ewma');\n\nif (result.dvol.dvolIndex > 75) {\n  console.log('⚠️  ALERTE: Volatilité élevée!');\n  // Ajuster le portefeuille\n}\n```\n\n### Comparaison temps réel\n\n```typescript\nconst comp = await analyzer.compare('bitcoin', 'solana', '30d');\n\nif (comp.comparison.percentageDifference > 30) {\n  console.log('Différence significative de volatilité');\n}\n```\n\n### Backtesting multi-période\n\n```typescript\nconst vol1d = await analyzer.analyze('bitcoin', '1d', 'ewma');\nconst vol30d = await analyzer.analyze('bitcoin', '30d', 'ewma');\nconst vol365d = await analyzer.analyze('bitcoin', '365d', 'ewma');\n\nif (vol1d.dvol.dvol > vol30d.dvol.dvol) {\n  console.log('Volatilité court-terme > moyen-terme');\n}\n```\n\n## 12. Formatter les résultats\n\n```typescript\nimport { ResultFormatter } from './examples/index.js';\n\nResultFormatter.separator('Mon Titre');\nResultFormatter.subsection('Sous-titre');\nResultFormatter.result('Label', value);\nResultFormatter.list(['Item 1', 'Item 2']);\nResultFormatter.success('Message de succès');\nResultFormatter.warning('Avertissement');\nResultFormatter.error('Message d\\'erreur');\n```\n\n## 13. Types TypeScript\n\n```typescript\ntype CryptoSymbol = 'bitcoin' | 'solana';\ntype TimePeriod = '1d' | '30d' | '365d';\ntype DVOLMethod = 'simple' | 'ewma' | 'garch';\n\ninterface VolatilityMetrics {\n  volatility: number;\n  variance: number;\n  annualizedVolatility: number;\n}\n\ninterface DVOLResult {\n  dvol: number;\n  dvolIndex: number;\n  confidence: number;\n  method: DVOLMethod;\n  dataPoints: number;\n  calculatedAt: Date;\n}\n```\n\n## 14. Commandes npm\n\n```bash\n# Build TypeScript\nnpm run build\n\n# Exemple simple\nnpm run example\n\n# Exemple usage\ntsx examples/usage.ts\n\n# Exemple avancé\ntsx examples/advanced.ts\n\n# Exemple TypeScript\ntsx examples/typescript.ts\n\n# Démarrage rapide\ntsx examples/quickstart.ts\n```\n\n## 15. Limites et notes\n\n- ⏱️ Taux limite: 10-50 requêtes/minute\n- 🔄 Délai automatique: 1.1s entre requêtes\n- 📊 Minimum 2 points pour volatilité, 10 pour GARCH\n- 🌍 Données: CoinGecko API (gratuite, sans clé)\n- 💾 Valeur: Que des nombres positifs\n\n## 16. Lien vers la doc complète\n\n- 📖 Guide: `INTEGRATION.md`\n- 📁 Exemples: `examples/README.md`\n- 📚 API: `src/`\n\n---\n\n**Besoin d'aide?** → Consultez les exemples ou la documentation complète!\n"