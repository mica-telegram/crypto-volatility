# 🚀 crypto-volatility - Guide d'intégration

Une librairie TypeScript moderne pour calculer la volatilité et le DVOL (Realized Volatility) des cryptomonnaies en utilisant l'API CoinGecko.

## 📦 Installation

```bash
npm install crypto-volatility
# ou
yarn add crypto-volatility
```

## ⚡ Quick Start (30 secondes)

```typescript
import { CoinGeckoProvider, VolatilityCalculator, DVOLCalculator } from 'crypto-volatility';\n\n// 1. Récupérer les données\nconst provider = new CoinGeckoProvider();\nconst prices = await provider.fetchPriceData('bitcoin', '30d');\n\n// 2. Calculer la volatilité\nconst volatility = VolatilityCalculator.calculateMetrics(prices.data, '30d');\n\n// 3. Calculer le DVOL\nconst dvol = DVOLCalculator.calculateDVOL(prices.data, 'ewma');\n\nconsole.log(`Volatilité: ${volatility.volatility.toFixed(2)}%`);\nconsole.log(`DVOL: ${dvol.dvol.toFixed(2)}%`);\n```\n\n## 📚 Utilisation basique\n\n### Volatilité standard\n\n```typescript\nimport { VolatilityCalculator } from 'crypto-volatility';\n\nconst metrics = VolatilityCalculator.calculateMetrics(priceData, '30d');\n\nconsole.log(metrics);\n// {\n//   volatility: 2.14,          // Volatilité en %\n//   variance: 45.78,            // Variance\n//   annualizedVolatility: 37.21 // Volatilité annualisée en %\n// }\n```\n\n### DVOL (Realized Volatility)\n\n```typescript\nimport { DVOLCalculator } from 'crypto-volatility';\n\nconst dvol = DVOLCalculator.calculateDVOL(priceData, 'ewma');\n\nconsole.log(dvol);\n// {\n//   dvol: 2.15,          // DVOL en %\n//   dvolIndex: 46.2,     // Index normalisé 0-100\n//   confidence: 91.2,    // Confiance du calcul %\n//   method: 'ewma',      // Méthode utilisée\n//   dataPoints: 30,      // Nombre de points\n//   calculatedAt: Date\n// }\n```\n\n### Trois méthodes DVOL disponibles\n\n```typescript\n// 1. Simple - Moyenne mobile\nconst dvol1 = DVOLCalculator.calculateDVOL(priceData, 'simple');\n\n// 2. EWMA - Exponentially Weighted Moving Average\nconst dvol2 = DVOLCalculator.calculateDVOL(priceData, 'ewma', {\n  ewmaLambda: 0.94\n});\n\n// 3. GARCH - Generalized Autoregressive Conditional Heteroskedasticity\nconst dvol3 = DVOLCalculator.calculateDVOL(priceData, 'garch', {\n  garchParams: {\n    omega: 0.000001,\n    alpha: 0.1,\n    beta: 0.85\n  }\n});\n```\n\n## 🎯 Cas d'usage courants\n\n### 1. Surveillance de volatilité temps réel\n\n```typescript\nimport { VolatilityAnalyzer } from './examples/index.js';\n\nconst analyzer = new VolatilityAnalyzer();\n\n// Analyser Bitcoin\nconst btc = await analyzer.analyze('bitcoin', '1d', 'ewma');\n\nif (btc.dvol.dvol > 50) {\n  console.log('⚠️  Alerte: Haute volatilité détectée!');\n  // Mettre à jour le portefeuille\n}\n```\n\n### 2. Comparaison multi-crypto\n\n```typescript\nconst analyzer = new VolatilityAnalyzer();\n\nconst results = await analyzer.analyzeMultiple(\n  ['bitcoin', 'solana'],\n  '30d',\n  'ewma'\n);\n\nconsole.log(results.ranking);\n// Affiche le classement par volatilité\n```\n\n### 3. Détection d'anomalies\n\n```typescript\nconst analyzer = new VolatilityAnalyzer();\n\nconst anomalies = await analyzer.detectAnomalies('bitcoin', '30d', 2);\n\nconsole.log(`${anomalies.count} anomalies détectées (${anomalies.percentage.toFixed(1)}%)`);\n```\n\n### 4. Backtesting avec volatilité\n\n```typescript\nconst analyzer = new VolatilityAnalyzer();\n\n// Analyser pour différentes périodes\nconst vol1d = await analyzer.analyze('bitcoin', '1d', 'ewma');\nconst vol30d = await analyzer.analyze('bitcoin', '30d', 'ewma');\nconst vol365d = await analyzer.analyze('bitcoin', '365d', 'ewma');\n\n// Comparer et ajuster la stratégie\nif (vol1d.dvol.dvol > vol30d.dvol.dvol) {\n  console.log('Volatilité court terme élevée');\n}\n```\n\n## 🔧 Configuration avancée\n\n### Options DVOL\n\n```typescript\nconst dvol = DVOLCalculator.calculateDVOL(priceData, 'ewma', {\n  windowSize: 20,              // Taille fenêtre (Simple)\n  ewmaLambda: 0.94,            // Paramètre EWMA\n  garchParams: {               // Paramètres GARCH\n    omega: 0.000001,\n    alpha: 0.1,\n    beta: 0.85\n  },\n  annualizationFactor: 365     // Facteur annualisation\n});\n```\n\n### Diagnostics\n\n```typescript\nconst diagnostics = DVOLCalculator.calculateDiagnostics(\n  priceData,\n  'ewma'\n);\n\nconsole.log(diagnostics);\n// {\n//   autocorrelation: 0.05,      // Relation avec passé\n//   heteroskedasticity: 0.25,   // Volatilité changeante\n//   skewness: 0.34,             // Asymétrie\n//   kurtosis: 3.56              // Queues grasses\n// }\n```\n\n## 📊 Types TypeScript\n\n```typescript\nimport type {\n  CryptoSymbol,      // 'bitcoin' | 'solana'\n  TimePeriod,        // '1d' | '30d' | '365d'\n  DVOLMethod,        // 'simple' | 'ewma' | 'garch'\n  VolatilityMetrics,\n  DVOLResult,\n  PriceData,\n  DVOLMetrics\n} from 'crypto-volatility';\n```\n\n## 🎓 Concepts clés\n\n### Volatilité\nMesure de la fluctuation des prix. Calculée comme l'écart-type des rendements logarithmiques.\n\n**Interprétation:**\n- **< 20%**: Très faible volatilité\n- **20-40%**: Faible volatilité\n- **40-70%**: Volatilité modérée\n- **70-100%**: Haute volatilité\n- **> 100%**: Très haute volatilité\n\n### DVOL (Realized Volatility)\nVolatilité calculée à partir des données historiques réelles.\n\n**Index DVOL (0-100):**\n- **0-25**: Très faible\n- **25-50**: Faible\n- **50-75**: Modérée\n- **75-100**: Élevée\n\n### Confiance\nScore de qualité du calcul DVOL (0-100%).\n- **90-100%**: Excellent - Utiliser les résultats avec confiance\n- **70-90%**: Bon - Résultats fiables\n- **< 70%**: Faible - Résultats à valider\n\n### Trois méthodes DVOL\n\n| Méthode | Avantages | Inconvénients | Cas d'usage |\n|---------|-----------|--------------|-------------|\n| **Simple** | Rapide, intuitif | Moins précis | Surveillance rapide |\n| **EWMA** | Réactif, bon équilibre | Paramètres à ajuster | Temps réel |\n| **GARCH** | Plus précis, modèle avancé | Plus lent, complexe | Analyse précise |\n\n## 📁 Structure du projet\n\n```\ncrypto-volatility/\n├── src/\n│   ├── calculators/\n│   │   ├── volatility.ts    # Calculs de volatilité\n│   │   └── dvol.ts          # Calculs DVOL\n│   ├── providers/\n│   │   └── coingecko.ts     # Fournisseur CoinGecko\n│   ├── types.ts             # Types TypeScript\n│   └── index.ts\n├── examples/\n│   ├── quickstart.ts        # Démarrage rapide\n│   ├── simple.ts            # Exemple simple\n│   ├── usage.ts             # Exemple complet\n│   ├── advanced.ts          # Cas avancés\n│   ├── typescript.ts        # Utilisation TypeScript\n│   ├── index.ts             # Utilitaires\n│   └── README.md            # Guide des exemples\n└── dist/                    # Build compilée\n```\n\n## 🔗 API CoinGecko\n\nLa librairie utilise l'API publique gratuite de CoinGecko:\n- ✅ Pas de clé API requise\n- ✅ Pas d'authentification nécessaire\n- ⚠️ Limité à 10-50 appels/minute\n- ⚠️ Délai de 1.1s entre les requêtes (respecté automatiquement)\n\n### Périodes disponibles\n- **'1d'**: Données horaires (24 points)\n- **'30d'**: Données quotidiennes (30 points)\n- **'365d'**: Données quotidiennes (365 points)\n\n### Cryptos supportées\n- **'bitcoin'** (BTC)\n- **'solana'** (SOL)\n\n*Note: D'autres cryptos peuvent être ajoutées*\n\n## 🚀 Exécuter les exemples\n\n```bash\n# Démarrage rapide\nnpm run example\n\n# Exemple complet\ntsx examples/usage.ts\n\n# Exemples avancés\ntsx examples/advanced.ts\n\n# Utilisation TypeScript\ntsx examples/typescript.ts\n```\n\n## 📈 Comparaison des méthodes\n\n```typescript\nimport { VolatilityAnalyzer } from './examples/index.js';\n\nconst analyzer = new VolatilityAnalyzer();\n\n// Compare les trois méthodes\nconst analysis = await analyzer.analyze('bitcoin', '30d', 'ewma');\n\nconsole.log(analysis.dvol.confidence); // Qualité du calcul\n```\n\n## 🎯 Recommandations\n\n### Quelle méthode choisir?\n\n**Pour commencer**: EWMA (`'ewma'`)\n- Bon équilibre entre précision et performance\n- Plus réactif que Simple\n- Plus efficace que GARCH\n\n**Pour la précision**: GARCH (`'garch'`)\n- Meilleur pour l'analyse statistique\n- Capture la volatilité conditionnelle\n- Plus lent\n\n**Pour la vitesse**: Simple (`'simple'`)\n- Calcul rapide\n- Facile à comprendre\n- Moins de données nécessaires\n\n### Quelle période choisir?\n\n- **'1d'**: Trading court terme\n- **'30d'**: Trading moyen terme, risk management\n- **'365d'**: Analyse long terme, allocation d'actifs\n\n## ⚠️ Limitations\n\n1. **Données CoinGecko**: Historiques limitées\n2. **Cryptos limitées**: Bitcoin et Solana actuellement\n3. **Taux limite**: 10-50 requêtes/minute\n4. **Besoin minimum**: 2 points pour volatilité, 10 pour GARCH\n\n## 🤝 Support\n\n- 📖 Voir `/examples` pour plus de cas d'usage\n- 🐛 Ouvrir une issue sur GitHub\n- 💬 Rejoindre la communauté\n\n## 📄 Licence\n\nApache License 2.0\n\n## 🙏 Remerciements\n\n- [CoinGecko](https://www.coingecko.com/) pour l'API\n- Communauté TypeScript\n- Utilisateurs et contributeurs\n\n---\n\n**Prêt à commencer?** → Consultez les [exemples](./examples/README.md)\n"